apiVersion: v1
kind: Service
metadata:
  name: public-proxy
  labels:
    app: public-proxy
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: public-proxy