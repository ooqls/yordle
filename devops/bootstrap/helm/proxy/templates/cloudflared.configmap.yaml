apiVersion: v1
kind: ConfigMap
metadata:
  name: cloudflared-config
  labels:
    app: cloudflared
data:
  config.yaml: |
    tunnel: {{ .Values.tunnelUUID }}
    
    ingress:
      {{- toYaml .Values.ingress | nindent 6 }}
