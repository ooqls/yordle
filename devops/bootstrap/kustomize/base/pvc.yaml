apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: public-pvc
  namespace: proxy
spec:
  accessModes:
    - ReadWriteOnce
    - ReadOnlyMany
  resources:
    requests:
      storage: 10Gi
  storageClassName: nfs
